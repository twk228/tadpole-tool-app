// import http, { Response } from '@/utils/http';

export default {
  // 用户信息列表
  // async getUserList(param: any) {
  //   return await http.post<Response<any>>(`/weLinkUser/getUserList`, param);
  // },
  // // 获取用户信息
  // async getUserById(param: any) {
  //   return await http.get<Response<any>>(`/weLinkUser/getUserById`, param);
  // },
};
